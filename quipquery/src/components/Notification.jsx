const Notification = () => {
  const style = {
    padding: 10,
    backgroundColor: "lavender",
    marginBottom: 10
  }
  
  if (true) return null

  return (
    <div style={style}>
      
    </div>
  )
}

export default Notification
